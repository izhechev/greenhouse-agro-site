/**
 * Ставки за калкулатора на "Ориентировъчна цена".
 *
 * Основани на пазарно проучване на цени за ремонт на покриви в България
 * през 2026 г. (труд + материали, в лв.). Това НЕ са твоите реални цени —
 * редактирай числата тук, за да отразяват твоите разходи и надценка.
 * Промяна тук веднага се отразява в калкулатора на сайта.
 */

export const CURRENCY = "лв.";

// Основна услуга — лв. за квадратен метър (труд + материали)
export const RATE_PER_SQM = {
  keramichni: 60, // Нови керемиди (бетонни/циментови)
  bitumni: 45, // Битумни керемиди
  metalni: 55, // Метални керемиди (ламарина тип керемида)
  shlebach: 62, // Шлайбах (стояща фалцова ламарина)
  pretursvane: 18, // Пренареждане на съществуващи керемиди (само труд)
  hidro: 25, // Хидроизолация
  novPokriv: 120, // Нов покрив от нулата (конструкция + всичко)
  navesi: 150, // Навеси (метална/дървена конструкция)
} as const;

// Добавки към основната услуга — лв/м²
export const ADDON_PER_SQM = {
  folio: 8, // Ново подпокривно фолио
  skaraSingle: 10, // Скара от летви — единична
  skaraDouble: 15, // Скара от летви — двойна
};

// Допълнителни дейности
export const ADDON_RATES = {
  ulukPerLm: 19, // Улуци — лв/линеен метър
  kazanchePerBr: 35, // Казанче (улук) — лв/брой
  biloPerLm: 20, // Било/капаци на билото — лв/линеен метър
  stopBrPrice: 4, // Стопер за сняг (плочести покриви) — лв/брой
  stopLmPrice: 22, // Стопер за сняг (метални покриви) — лв/линеен метър
  stenaPerLm: 20, // Стенна ламарина — лв/линеен метър
  ulamaPerLm: 24, // Улама (вътрешен ъгъл) — лв/линеен метър
  naduluchnaPerLm: 10, // Надулучна пола — лв/линеен метър
  kalkadnaPerLm: 20, // Калканна ламарина — лв/линеен метър
  izvozvane: 120, // Извозване на строителни отпадъци — фиксна такса
};

// Обшивка на комин — по размер, лв/брой
export const KOMIN_PRICE = {
  small: 180,
  medium: 280,
  large: 450,
};

// Капандура — по вид, лв/брой (само рамка/обшивка, без цената на самия прозорец при Velux)
export const KAPANDURA_PRICE = {
  solid: 250,
  poly: 320,
  glass: 550,
  velux: 700,
};

// Диапазон на несигурност за "ориентировъчна" цена (± %)
export const ESTIMATE_MARGIN = 0.15;
