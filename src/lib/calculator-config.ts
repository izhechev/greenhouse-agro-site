/**
 * Ставки за калкулатора на "Ориентировъчна цена".
 *
 * Основани на пазарно проучване на цени за ремонт на покриви в България
 * през 2026 г. (труд + материали, в евро). Това НЕ са твоите реални цени —
 * редактирай числата тук, за да отразяват твоите разходи и надценка.
 * Промяна тук веднага се отразява в калкулатора на сайта.
 */

export const CURRENCY = "€";

// Основна услуга — евро за квадратен метър (труд + материали)
export const RATE_PER_SQM = {
  keramichni: 30, // Нови керемиди (бетонни/циментови)
  bitumni: 23, // Битумни керемиди
  metalni: 28, // Метални керемиди (ламарина тип керемида)
  shlebach: 32, // Шлайбах (стояща фалцова ламарина)
  pretursvane: 9, // Пренареждане на съществуващи керемиди (само труд)
  hidro: 13, // Хидроизолация
  novPokriv: 60, // Нов покрив от нулата (конструкция + всичко)
  navesi: 75, // Навеси (метална/дървена конструкция)
} as const;

// Добавки към основната услуга — евро/м²
export const ADDON_PER_SQM = {
  folio: 4, // Ново подпокривно фолио
  skaraSingle: 5, // Скара от летви — единична
  skaraDouble: 8, // Скара от летви — двойна
};

// Допълнителни дейности
export const ADDON_RATES = {
  ulukPerLm: 10, // Улуци — евро/линеен метър
  kazanchePerBr: 18, // Казанче (улук) — евро/брой
  biloPerLm: 10, // Било/капаци на билото — евро/линеен метър
  stopBrPrice: 2, // Стопер за сняг (плочести покриви) — евро/брой
  stopLmPrice: 11, // Стопер за сняг (метални покриви) — евро/линеен метър
  stenaPerLm: 10, // Стенна ламарина — евро/линеен метър
  ulamaPerLm: 12, // Улама (вътрешен ъгъл) — евро/линеен метър
  naduluchnaPerLm: 5, // Надулучна пола — евро/линеен метър
  kalkadnaPerLm: 10, // Калканна ламарина — евро/линеен метър
  izvozvane: 60, // Извозване на строителни отпадъци — фиксна такса
};

// Обшивка на комин — по размер, евро/брой
export const KOMIN_PRICE = {
  small: 90,
  medium: 140,
  large: 230,
};

// Капандура — по вид, евро/брой (само рамка/обшивка, без цената на самия прозорец при Velux)
export const KAPANDURA_PRICE = {
  solid: 130,
  poly: 165,
  glass: 280,
  velux: 360,
};

// Диапазон на несигурност за "ориентировъчна" цена (± %)
export const ESTIMATE_MARGIN = 0.15;
